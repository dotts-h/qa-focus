{
  "name": "qa-focus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control-first agentic QA on the Copilot SDK + Playwright: an autonomous explorer that discovers, and a gated codifier that hardens findings into standards-compliant durable tests.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "playwright test",
    "lint": "for f in bin/*.mjs src/*.mjs extension/qa-focus/*.mjs; do node --check \"$f\" || exit 1; done && bash scripts/check-workflows.sh",
    "doctor": "C=\"${COOKBOOK:-$CLAUDE_PLUGIN_ROOT/cookbook}\"; if [ -d \"$C/recipes\" ]; then for r in core quality security issues loop contracts release; do bash \"$C/recipes/$r/scripts/doctor.sh\" .; done; else echo 'recipe-doctor: set COOKBOOK to the cookbook plugin dir to run conformance checks (skipped)'; fi",
    "explore": "node bin/explore.mjs",
    "codify": "node bin/codify.mjs",
    "interactive": "node bin/interactive.mjs"
  },
  "dependencies": {
    "@github/copilot-sdk": "*",
    "@playwright/test": "*",
    "playwright": "*"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/cli": "^0.1.14"
  }
}
